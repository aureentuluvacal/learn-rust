// Can setup specific configs for dev and prod in the Cargo.toml.

// Can use documentation comments (///) that generates HTML for
// how to use your crate. It uses markdown. Running `cargo doc --open`
// will generate the HTML and open it in the browser.

// Can use //! to document the crate root in lib.rs.

// Can separate work into separate crates to simplify projects before they get
// too big using workspaces. Set up the config in the Cargo.toml.
